# Obsidian Setup

After cloning this repo:
1. Install [Obsidian](https://obsidian.md)
2. Open this folder as a vault
3. Go to Settings → Community Plugins → install **Local REST API with MCP**
4. Generate an API key in plugin settings → copy to `~/.zshrc` as `OBSIDIAN_REST_API_KEY`
